<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hkust.mapper.wmsc.WmsStocktakingRecordMapper">
    <insert id="batchInsertStocktakingRecord" parameterType="list">
        INSERT INTO wms_stocktaking_record (id, name, count, operator_id, operator, create_time, update_time, remark)
        VALUES
        <foreach collection="list" item="record" separator=",">
            (#{record.id},
            #{record.name},
            #{record.count},
            #{record.operatorId},
            #{record.operator},
            #{record.create_time},
            #{record.update_time},
            #{record.remark})
        </foreach>
    </insert>
</mapper>