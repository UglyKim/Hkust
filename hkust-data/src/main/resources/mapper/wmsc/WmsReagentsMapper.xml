<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hkust.mapper.wmsc.WmsReagentsMapper">
    <select id="selectReagentsGroupedByNameAndSpecification" resultType="java.util.HashMap">
        <![CDATA[
        SELECT
            name,
            specification,
            COUNT(*) AS total_count
        FROM
            wms_reagents
        WHERE
            create_time >= #{startDate}
          AND create_time <= #{endDate}
        GROUP BY
            name,
            specification
        LIMIT #{limit} OFFSET #{offset}
        ]]>
    </select>
</mapper>